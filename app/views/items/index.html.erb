<%= javascript_include_tag "items.js", "data-turbolinks-track" => false %>
<div class="container">

	<div class="help-text">
		<p>To change the amount, click on the edit button. This will bring you to a screen to edit the name, amount or price of an item. Click on the column names Item name, amount, or price to order the table.
		</p>
	</div>
	<br />
	<h1>Items List</h1>
	<div class="table-responsive">
		<table class="table table-hover">
			<tr>
				<td><b><%= link_to "Item Name", :sort => "item_name" %><span class="help"> ?</span></b></td>
				<td><b><%= link_to "Amount", :sort => "amount" %></b></td>
				<td><b><%= link_to "Price", :sort => "price" %></b></td>
				<td><b>Updated Last By</b></td>
				<td><b>Edit Item</b></td>
				<td><b>Remove Item</b></td>
			</tr>
			<% @items.each do |item| %>
			<tr>
				<td><%= link_to item.item_name, item_path(item.id) %></td>
				<td><%=item.amount%></td>
				<td><%=number_to_currency(item.price)%></td>
				<td><%= link_to item.user.name, user_path(item.user.id) %></td>
				<td><%=link_to "Edit Item", edit_item_path(item.id), :class => "btn btn-primary btn-sm" %></td>
				<td><%= link_to "Remove Item", item, confirm: "Are you sure you want to delete this item?", method: :delete, :class => "btn btn-primary btn-sm" %></td>
			</tr>
			<%end%>
		</table>
	</div>
</div>